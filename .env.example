# Copia este archivo a `.env.local` y completa los valores desde el panel de Supabase (Settings → API).

# URL del proyecto (Auth, Storage, Postgres)
VITE_SUPABASE_URL=https://xglltbdqptlzxdynckds.supabase.co

# Clave anon (segura para el navegador; gobierna Row Level Security)
VITE_SUPABASE_ANON_KEY=

# Opcional más adelante: base URL del API Nest cuando conectemos el frontend
# VITE_API_URL=http://localhost:4000
