# Copia este archivo a `.env` y ajusta los valores.

# URL del backend NestJS (rutek-api). Reinicia Vite tras cambiar esto.
# Docker local: http://localhost:4000
# Windows si localhost falla: http://127.0.0.1:4000
VITE_API_URL=http://localhost:4000
